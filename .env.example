# Cloudinary Configuration
# Set your Cloudinary cloud name (publicly accessible on the client side)
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your-cloud-name

# Cloudinary API Credentials (keep secret - only needed for server-side/admin SDK usage)
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret
